name: example
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.6.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  cupertino_icons:
  cloud_firestore:
  cloud_functions:
  subscription_holder: any
  provider:
  firebase_auth:
  firestore_ref:
    path: ..
  recase:
  state_notifier:
  flutter_state_notifier:
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any
  freezed: any
  json_serializable: any
  pedantic_mono: any
dependency_overrides:
  firebase_core: ^0.5.0-dev
flutter:
  uses-material-design: true
